<div class="container-fluid" style="padding-top: 70px;">
  <h1 class="text-center"><%= @user.name %></h1>
  <hr />
  <div class="row">
    <div class="col-md-6">
      <h2 class="text-center">Watched Movies</h2>
      <hr />
      <% if @user.watched_movies.empty? %>
        <p class="text-center">Looks like there are no watched movies! Sorry!</p>
      <% else %>
        <% @user.watched_movies.in_groups_of(3).each do |movie_slices| %>
          <div class="row">
            <% movie_slices.each do |movie| %>
              <% if movie %>
                <div class="col-md-4">
                  <h3 class="text-center"><%= link_to "#{truncate movie.title, :length => 16, :separator => /\w/, :omission => "..."}".html_safe, movie_path(movie.api_id) %></h3>
                  <hr />
                  <a href="<%= movie_path(movie.api_id) %>"><img style="margin: 0 auto" class="media-object" src="<%= movie.thumbnail %>" alt="movie poster" height="200"></a>
                </div>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="col-md-6">
      <h2 class="text-center">Unwatched Movies</h2>
      <hr />
      <% if @user.unwatched_movies.empty? %>
        <p class="text-center">Looks like there are no unwatched movies! Sorry!</p>
      <% else %>
        <% @user.unwatched_movies.in_groups_of(3).each do |movie_slices| %>
          <div class="row">
            <% movie_slices.each do |movie| %>
              <% if movie %>
                <div class="col-md-4">
                  <h3 class="text-center"><%= link_to "#{truncate movie.title, :length => 16, :separator => /\w/, :omission => "..."}".html_safe, movie_path(movie.api_id) %></h3>
                  <hr />
                  <a href="<%= movie_path(movie.api_id) %>"><img style="margin: 0 auto" class="media-object" src="<%= movie.thumbnail %>" alt="movie poster" height="200"></a>
                </div>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
  <% unless @user.shared_unwatched_movies.empty? %>
    <hr style="padding-top: 20px" />
    <h2 class="text-center">Watch these movies with your friends!</h2>
    <hr />
    <div class="row" style="padding-bottom: 20px">
      <% @user.shared_unwatched_movies.each do |movie| %>
        <div class="col-md-3">
          <h3 class="text-center"><%= truncate movie["friend"], :length => 20, :separator => /\w/, :omission => "..." %></h3>
          <hr />
          <a href="<%= movie_path(movie["movie"].api_id) %>"><img style="margin: 0 auto" class="media-object" src="<%= movie["movie"].thumbnail %>" alt="movie poster" height="200"></a>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
